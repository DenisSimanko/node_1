<%  %>

<h2>Index Page</h2>

<% for(let blog of blogs) { %>
  <div>
    <h3><%= blog.title %></h3>
    <img src="<%= blog.image %>" alt="<%= blog.title %>">
    <p><%= blog.body %></p>
    <p><%= blog.created %></p>
  </div>
<% } %>

<%  %>